version: '2.3'

services:
  database:
    ports:
      - 3306:3306
    logging:
      driver: json-file

  blockchain:
    ports:
      - 8545:8545
    logging:
      driver: json-file

  server:
    ports:
      - 3000:3000
    logging:
      driver: json-file
  web:
    ports:
      - 80:3000
      - 8080:80
    logging:
      driver: json-file
